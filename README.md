The Designer project is a repro for our projects using the old `PublicResXFileCodeGenerator`.

The Generator project is a repro for our projects using the new `Microsoft.CodeAnalysis.ResxSourceGenerator`.

